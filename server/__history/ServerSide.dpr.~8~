program ServerSide;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  Horse,
  Horse.Jhonson,
  Horse.CORS,
  Horse.SocketIO;

begin
  THorse
    .Use(Jhonson)
    .Use(CORS)
    .Use(SocketIO(8080));

  THorse.Listen(9000);
end.
